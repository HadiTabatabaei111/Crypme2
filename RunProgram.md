# راهنمای مرحله به مرحله راه‌اندازی ربات ترید کریپتو

این راهنما برای راه‌اندازی ربات روی لپ‌تاپ ویندوز شما طراحی شده است.

## مرحله ۱: بررسی پیش‌نیازها

### ۱.۱ بررسی نصب Python

در PowerShell یا Command Prompt تایپ کنید:

```powershell
python --version
```

**اگر Python نصب نیست:**
- به سایت https://www.python.org/downloads/ بروید
- آخرین نسخه Python 3.8 یا بالاتر را دانلود کنید
- هنگام نصب، حتماً گزینه "Add Python to PATH" را تیک بزنید
- بعد از نصب، PowerShell را ببندید و دوباره باز کنید

### ۱.۲ بررسی نصب pip

```powershell
python -m pip --version
```

اگر خطا داد، pip را نصب کنید:

```powershell
python -m ensurepip --upgrade
```

---

## مرحله ۲: آماده‌سازی پروژه

### ۲.۱ رفتن به پوشه پروژه

در PowerShell به پوشه پروژه بروید:

```powershell
cd D:\App\cryptome
```

### ۲.۲ بررسی فایل‌های پروژه

مطمئن شوید این فایل‌ها وجود دارند:
- `requirements.txt`
- `config.json.example`
- `crypto_futures_bot.py`
- `setup_and_run.bat`

---

## مرحله ۳: نصب وابستگی‌ها

### روش ۱: استفاده از اسکریپت خودکار (توصیه می‌شود)

فقط این دستور را اجرا کنید:

```powershell
.\setup_and_run.bat
```

این اسکریپت به صورت خودکار:
- Python و pip را بررسی می‌کند
- تمام پکیج‌های لازم را نصب می‌کند
- فایل `config.json` را از روی `config.json.example` می‌سازد

### روش ۲: نصب دستی

اگر می‌خواهید دستی نصب کنید:

```powershell
# به‌روزرسانی pip
python -m pip install --upgrade pip

# نصب وابستگی‌ها
python -m pip install -r requirements.txt
```

---

## مرحله ۴: تنظیم فایل کانفیگ

### ۴.۱ ایجاد فایل config.json

اگر فایل `config.json` وجود ندارد، از روی نمونه بسازید:

```powershell
copy config.json.example config.json
```

### ۴.۲ ویرایش فایل config.json

فایل `config.json` را با Notepad یا هر ویرایشگر متنی باز کنید:

```powershell
notepad config.json
```

**تنظیمات مهم:**

```json
{
  "scan_interval": 300,           // فاصله اسکن (ثانیه) - 5 دقیقه
  "min_confidence": 0.7,           // حداقل اعتماد برای سیگنال
  "max_position_size": 100,        // حداکثر حجم معامله (USDT)
  "stop_loss_percent": 5,          // درصد Stop Loss
  "take_profit_percent": 10,       // درصد Take Profit
  "auto_trade": false,             // ⚠️ برای شروع: false (فقط تحلیل)
  "testnet": true                  // ⚠️ برای شروع: true (تست)
}
```

**⚠️ نکته مهم:** برای شروع، `auto_trade` را `false` بگذارید تا فقط تحلیل کند و معامله نکند.

---

## مرحله ۵: تنظیم API Keys (اختیاری)

### اگر می‌خواهید معامله خودکار انجام دهید:

**روش ۱: استفاده از متغیرهای محیطی (توصیه می‌شود - امن‌تر)**

در PowerShell:

```powershell
# برای این جلسه PowerShell
$env:BITUNIX_API_KEY="your_api_key_here"
$env:BITUNIX_API_SECRET="your_api_secret_here"
```

**برای دائمی کردن (اختیاری):**

```powershell
# اضافه کردن به متغیرهای محیطی سیستم
[System.Environment]::SetEnvironmentVariable("BITUNIX_API_KEY", "your_api_key_here", "User")
[System.Environment]::SetEnvironmentVariable("BITUNIX_API_SECRET", "your_api_secret_here", "User")
```

**روش ۲: اضافه کردن به config.json (کمتر امن)**

```json
{
  "bitunix_api_key": "YOUR_API_KEY",
  "bitunix_api_secret": "YOUR_API_SECRET",
  "bitunix_testnet": false
}
```

⚠️ **هشدار:** اگر از config.json استفاده می‌کنید، هرگز این فایل را در Git یا جاهای عمومی قرار ندهید!

---

## مرحله ۶: اجرای ربات

### حالت ۱: فقط تحلیل (بدون معامله) - توصیه برای شروع

```powershell
python crypto_futures_bot.py
```

این حالت فقط بازار را تحلیل می‌کند و سیگنال‌ها را نشان می‌دهد، اما معامله نمی‌کند.

### حالت ۲: اجرا با اسکریپت

```powershell
.\setup_and_run.bat --run
```

---

## مرحله ۷: بررسی لاگ‌ها

برای مشاهده لاگ‌های ربات:

```powershell
# مشاهده آخرین خطوط لاگ
Get-Content trading_bot.log -Tail 50

# مشاهده لاگ‌های زنده (در حال اجرا)
Get-Content trading_but.log -Wait -Tail 20
```

---

## مرحله ۸: راه‌اندازی داشبورد وب (اختیاری)

اگر می‌خواهید داشبورد وب را هم ببینید:

```powershell
python dashboard_server.py
```

سپس در مرورگر به آدرس زیر بروید:
```
http://localhost:5000
```

---

## عیب‌یابی مشکلات رایج

### مشکل ۱: "Python is not recognized"

**راه حل:**
- Python را دوباره نصب کنید
- هنگام نصب، گزینه "Add Python to PATH" را تیک بزنید
- PowerShell را ببندید و دوباره باز کنید

### مشکل ۲: "ModuleNotFoundError"

**راه حل:**
```powershell
python -m pip install -r requirements.txt
```

### مشکل ۳: "config.json not found"

**راه حل:**
```powershell
copy config.json.example config.json
```

### مشکل ۴: "Permission denied" یا خطای دسترسی

**راه حل:**
- PowerShell را به عنوان Administrator اجرا کنید
- یا از Command Prompt استفاده کنید

### مشکل ۵: اتصال به اینترنت

**راه حل:**
- اتصال اینترنت را بررسی کنید
- اگر پشت فایروال هستید، ممکن است نیاز به تنظیمات خاص باشد

---

## نکات مهم امنیتی

1. ⚠️ **همیشه با Testnet شروع کنید**: `"testnet": true`
2. ⚠️ **در ابتدا فقط تحلیل کنید**: `"auto_trade": false`
3. ⚠️ **API Keys را محرمانه نگه دارید**
4. ⚠️ **فقط سرمایه‌ای که توان از دست دادنش را دارید استفاده کنید**
5. ⚠️ **ربات را همیشه تحت نظر داشته باشید**

---

## مراحل بعدی

بعد از راه‌اندازی موفق:

1. ✅ ربات را در حالت تحلیل اجرا کنید
2. ✅ لاگ‌ها را بررسی کنید
3. ✅ سیگنال‌ها را در داشبورد مشاهده کنید
4. ✅ بعد از اطمینان، می‌توانید `auto_trade` را `true` کنید

---

## پشتیبانی

اگر مشکلی پیش آمد:
1. لاگ‌ها را بررسی کنید (`trading_bot.log`)
2. خطاها را در PowerShell بررسی کنید
3. مطمئن شوید تمام مراحل را درست انجام داده‌اید

---

**موفق باشید! 🚀**

