# داشبورد ترید کریپتو - Crypto Trading Dashboard

داشبورد مدرن و زیبا برای نمایش اطلاعات ارزهای دیجیتال با قابلیت‌های فیلترینگ، نمایش چارت و شناسایی ارزهای پامپ و دامپ.

## ویژگی‌ها

### 📊 نمایش اطلاعات
- نمایش لیست کامل ارزها با اطلاعات کامل
- آمار کلی در هدر (کل ارزها، پامپ‌ها، دامپ‌ها، سیگنال‌ها)
- نمایش ارزهای پامپ (افزایش قیمت >15%)
- نمایش ارزهای دامپ (کاهش قیمت <-15%)

### 🔍 فیلترینگ پیشرفته
- جستجو بر اساس نماد یا نام ارز
- فیلتر بر اساس نوع (پامپ، دامپ، پامپ شدید، دامپ شدید)
- فیلتر بر اساس تغییر قیمت (حداقل و حداکثر)
- امکان ترکیب فیلترها

### 📈 چارت‌ها
- نمودار تغییرات قیمت (24 ساعت)
- نمودار توزیع تغییرات قیمت
- چارت قیمت برای هر ارز (در مودال)
- به‌روزرسانی خودکار

### 🎨 رابط کاربری
- طراحی مدرن و زیبا با تم تاریک
- واکنش‌گرا (Responsive) - سازگار با موبایل
- پشتیبانی از زبان فارسی (RTL)
- انیمیشن‌های نرم

## نصب و راه‌اندازی

### پیش‌نیازها
- Python 3.8 یا بالاتر
- دیتابیس `trading_bot.db` (باید از قبل توسط ربات ایجاد شده باشد)

### نصب

#### روش 1: استفاده از اسکریپت (توصیه می‌شود)

**Windows:**
```bash
run_dashboard.bat
```

**Linux/Mac:**
```bash
chmod +x run_dashboard.sh
./run_dashboard.sh
```

#### روش 2: نصب دستی

```bash
# نصب وابستگی‌ها
pip install -r requirements.txt

# اجرای سرور
python dashboard_server.py
```

### دسترسی به داشبورد

پس از اجرای سرور، داشبورد در آدرس زیر در دسترس است:
```
http://localhost:5000
```

## استفاده

### مشاهده ارزها
1. لیست کامل ارزها در جدول اصلی نمایش داده می‌شود
2. می‌توانید با استفاده از pagination بین صفحات حرکت کنید

### فیلتر کردن
1. از بخش فیلترها در بالای صفحه استفاده کنید
2. می‌توانید بر اساس:
   - نوع (پامپ/دامپ)
   - تغییر قیمت
   - جستجوی متنی
   فیلتر کنید
3. روی دکمه "اعمال فیلتر" کلیک کنید

### مشاهده چارت
1. در جدول ارزها، روی دکمه "نمایش چارت" کلیک کنید
2. چارت قیمت 24 ساعته در یک مودال نمایش داده می‌شود

### مشاهده ارزهای پامپ و دامپ
- بخش "ارزهای پامپ" در بالای صفحه، ارزهایی با افزایش قیمت شدید را نمایش می‌دهد
- بخش "ارزهای دامپ" در بالای صفحه، ارزهایی با کاهش قیمت شدید را نمایش می‌دهد

## API Endpoints

### GET /api/tokens
دریافت لیست ارزها با فیلترها

**پارامترها:**
- `limit`: تعداد رکورد در هر صفحه (پیش‌فرض: 100)
- `offset`: تعداد رکوردهای رد شده (پیش‌فرض: 0)
- `filter`: نوع فیلتر (pump, dump, pumped, dumped)
- `search`: متن جستجو
- `min_price_change`: حداقل تغییر قیمت
- `max_price_change`: حداکثر تغییر قیمت

**مثال:**
```
GET /api/tokens?filter=pump&limit=50
```

### GET /api/price-history/<token_address>
دریافت تاریخچه قیمت یک ارز

**پارامترها:**
- `hours`: تعداد ساعت برای نمایش (پیش‌فرض: 24)

**مثال:**
```
GET /api/price-history/0x123...?hours=48
```

### GET /api/statistics
دریافت آمار کلی داشبورد

**مثال:**
```
GET /api/statistics
```

### GET /api/pumped
دریافت ارزهای پامپ

**پارامترها:**
- `limit`: تعداد ارزها (پیش‌فرض: 50)

**مثال:**
```
GET /api/pumped?limit=20
```

### GET /api/dumped
دریافت ارزهای دامپ

**پارامترها:**
- `limit`: تعداد ارزها (پیش‌فرض: 50)

**مثال:**
```
GET /api/dumped?limit=20
```

## ساختار فایل‌ها

```
.
├── dashboard_server.py      # سرور Flask
├── templates/
│   └── dashboard.html       # قالب HTML
├── static/
│   ├── css/
│   │   └── dashboard.css    # استایل‌های CSS
│   └── js/
│       └── dashboard.js     # کد JavaScript
├── run_dashboard.bat        # اسکریپت اجرا (Windows)
├── run_dashboard.sh         # اسکریپت اجرا (Linux/Mac)
└── requirements.txt         # وابستگی‌ها
```

## تنظیمات

### تغییر پورت
در فایل `dashboard_server.py`، خط آخر را تغییر دهید:
```python
app.run(debug=True, host='0.0.0.0', port=5000)  # پورت را تغییر دهید
```

### تغییر مسیر دیتابیس
از متغیر محیطی استفاده کنید:
```bash
export DB_PATH=/path/to/your/database.db
python dashboard_server.py
```

یا در کد تغییر دهید:
```python
DB_PATH = os.getenv('DB_PATH', 'trading_bot.db')
```

## به‌روزرسانی خودکار

داشبورد به طور خودکار هر 30 ثانیه یک‌بار به‌روزرسانی می‌شود. می‌توانید این زمان را در فایل `static/js/dashboard.js` تغییر دهید:

```javascript
// Auto refresh every 30 seconds
setInterval(() => {
    loadStatistics();
    loadTokens();
    loadPumpedTokens();
    loadDumpedTokens();
}, 30000);  // زمان را به میلی‌ثانیه تغییر دهید
```

## عیب‌یابی

### مشکل: دیتابیس پیدا نمی‌شود
- مطمئن شوید که ربات را حداقل یک‌بار اجرا کرده‌اید
- مسیر دیتابیس را بررسی کنید

### مشکل: داشبورد باز نمی‌شود
- بررسی کنید که پورت 5000 آزاد است
- لاگ‌های سرور را بررسی کنید

### مشکل: داده‌ها نمایش داده نمی‌شوند
- بررسی کنید که دیتابیس شامل داده است
- کنسول مرورگر را برای خطاهای JavaScript بررسی کنید

## ویژگی‌های آینده

- [ ] فیلتر بر اساس حجم معاملات
- [ ] فیلتر بر اساس نقدینگی
- [ ] صادرات داده‌ها به Excel/CSV
- [ ] تنظیمات کاربری
- [ ] نمودارهای پیشرفته‌تر
- [ ] اعلان‌های لحظه‌ای
- [ ] چندزبانه کردن

## پشتیبانی

برای مشکلات و سوالات:
1. بررسی لاگ‌های سرور
2. بررسی کنسول مرورگر
3. بررسی مستندات

## مجوز

این پروژه برای استفاده آموزشی و تحقیقاتی است.

---

**نویسنده**: Advanced Crypto Trading Bot Team  
**نسخه**: 1.0  
**تاریخ**: 2024

