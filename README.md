# ربات ترید فیوچرز کریپتو - Advanced Crypto Futures Trading Bot

ربات حرفه‌ای معامله‌گری فیوچرز کریپتو با قابلیت‌های پیشرفته برای تشخیص پامپ، دامپ و معاملات خودکار.

## ویژگی‌ها

### 🔍 تحلیل و تشخیص
- **ادغام با DexScreener**: اسکن خودکار توکن‌ها و جفت‌های معاملاتی جدید
- **تشخیص پامپ (Pump Detection)**: شناسایی افزایش قیمت ناگهانی
- **تشخیص دامپ (Dump Detection)**: شناسایی کاهش قیمت شدید
- **تشخیص Shadow Pump**: شناسایی حجم جعلی و دستکاری قیمت
- **بررسی RugCheck**: بررسی امنیت توکن‌ها قبل از معامله
- **لیست سیاه**: اضافه کردن توکن‌های مشکوک به لیست سیاه

### 💾 ذخیره‌سازی داده
- **دیتابیس SQLite**: ذخیره اطلاعات توکن‌ها، قیمت‌ها، سیگنال‌ها و معاملات
- **تاریخچه قیمت**: ذخیره تاریخچه قیمت برای تحلیل الگوها
- **تحلیل الگوها**: شناسایی و ذخیره الگوهای معاملاتی

### 🤖 معامله خودکار
- **ادغام Bitunix Futures**: معاملات دستی/اتوماتیک در Bitunix (USDT Perpetual)
- **مدیریت ریسک**: Stop Loss و Take Profit خودکار
- **مدیریت موقعیت**: نظارت بر موقعیت‌های باز

## نصب و راه‌اندازی

### پیش‌نیازها
- Python 3.8 یا بالاتر
- pip3
- حساب Bitunix با API Key و Secret (اختیاری - برای معامله)

### نصب سریع

```bash
# دانلود پروژه
cd "New folder"

# اجرای اسکریپت نصب
chmod +x setup_and_run.sh
./setup_and_run.sh
```

### نصب دستی

```bash
# نصب وابستگی‌ها
pip3 install -r requirements.txt

# کپی فایل کانفیگ
cp config.json.example config.json

# ویرایش کانفیگ
nano config.json
```

## پیکربندی

### تنظیم API Keys (ایمن و بدون تحریم)

1. **Bitunix API Keys (اختیاری برای ارسال سفارش)**:
   - وارد حساب Bitunix شوید
   - API Key و Secret بسازید و دسترسی Futures را فعال کنید

2. **استفاده از متغیرهای محیطی (توصیه‌شده)**:
```bash
# Windows PowerShell
$env:BITUNIX_API_KEY="your_api_key"
$env:BITUNIX_API_SECRET="your_api_secret"

# Linux/macOS
export BITUNIX_API_KEY="your_api_key"
export BITUNIX_API_SECRET="your_api_secret"
```

3. **(اختیاری) افزودن به config.json**:
```json
{
  "bitunix_api_key": "YOUR_API_KEY",
  "bitunix_api_secret": "YOUR_API_SECRET",
  "bitunix_testnet": false
}
```

توجه: کلیدها در لاگ‌ها چاپ نمی‌شوند و توصیه می‌شود فقط از متغیر محیطی استفاده کنید.

### تنظیمات مهم

در فایل `config.json`:

- `scan_interval`: فاصله زمانی اسکن (ثانیه) - پیش‌فرض: 300 (5 دقیقه)
- `min_confidence`: حداقل اعتماد برای سیگنال (0.0 تا 1.0) - پیش‌فرض: 0.7
- `max_position_size`: حداکثر حجم معامله (USDT) - پیش‌فرض: 100
- `stop_loss_percent`: درصد Stop Loss - پیش‌فرض: 5%
- `take_profit_percent`: درصد Take Profit - پیش‌فرض: 10%
- `auto_trade`: فعال‌سازی معامله خودکار - پیش‌فرض: false
- `testnet`: استفاده از ByBit Testnet - پیش‌فرض: true

## استفاده

### حالت تحلیل (بدون معامله)

```bash
# در config.json: "auto_trade": false
python3 crypto_futures_bot.py
```

### حالت معامله خودکار

```bash
# صفحه تنظیمات: /auto-trade را پیکربندی کنید
python3 crypto_futures_bot.py
```

### اجرا با اسکریپت

```bash
./setup_and_run.sh --run
```

## ساختار دیتابیس

### جداول اصلی

1. **tokens**: اطلاعات توکن‌ها
   - آدرس، نماد، قیمت، حجم، نقدینگی
   - وضعیت لیست سیاه

2. **price_history**: تاریخچه قیمت
   - قیمت و حجم در زمان‌های مختلف

3. **trading_signals**: سیگنال‌های معاملاتی
   - نوع سیگنال (LONG/SHORT)
   - قیمت ورود، Stop Loss، Take Profit
   - سطح اعتماد

4. **trades**: معاملات انجام شده
   - وضعیت معامله (OPEN/CLOSED)
   - سود/زیان

5. **blacklist**: لیست سیاه
   - توکن‌های مشکوک و دلیل اضافه شدن

6. **patterns**: الگوهای شناسایی شده

## الگوریتم‌های تشخیص

### تشخیص پامپ
- افزایش قیمت بیش از 15% در بازه کوتاه
- افزایش حجم معاملات
- نقدینگی مناسب

### تشخیص دامپ
- کاهش قیمت بیش از 15%
- کاهش شدید حجم یا نقدینگی

### تشخیص Shadow Pump
- نسبت حجم به نقدینگی غیرطبیعی (>10)
- نوسانات بالا با نقدینگی کم
- استفاده از API Pocker Universe (ادغام شده در DexScreener)

### بررسی RugCheck
- بررسی هانی‌پات (Honeypot)
- بررسی قابلیت مینت شدن
- بررسی قفل نقدینگی
- بررسی تعداد دارندگان

## نکات امنیتی

1. **همیشه از Testnet شروع کنید**: قبل از معامله واقعی، در Testnet تست کنید
2. **محدود کردن API Keys**: فقط دسترسی‌های لازم را به API Key بدهید
3. **عدم استفاده از تمام سرمایه**: فقط قسمتی از سرمایه را برای معامله خودکار استفاده کنید
4. **نظارت مداوم**: ربات را تحت نظر داشته باشید
5. **Backup دیتابیس**: به طور منظم از دیتابیس بکاپ بگیرید

## لاگ‌ها

لاگ‌ها در فایل `trading_bot.log` ذخیره می‌شوند:

```bash
# مشاهده لاگ‌های زنده
tail -f trading_bot.log

# جستجو در لاگ‌ها
grep "Signal" trading_bot.log
grep "ERROR" trading_bot.log
```

## عیب‌یابی

### مشکل: API Key کار نمی‌کند
- بررسی کنید که API Key و Secret درست وارد شده باشد
- بررسی کنید که دسترسی Futures Trading فعال باشد
- اگر از Testnet استفاده می‌کنید، API Key را از Testnet دریافت کنید

### مشکل: توکن‌ها پیدا نمی‌شوند
- بررسی اتصال اینترنت
- بررسی دسترسی به DexScreener API
- بررسی لاگ‌ها برای خطاها

### مشکل: معاملات انجام نمی‌شوند
- بررسی کنید که `auto_trade` روی `true` باشد
- بررسی کنید که نماد در Bitunix Futures موجود باشد
- بررسی موجودی حساب

## توسعه و سفارشی‌سازی

### منابع داده ضدتحریم
- قیمت و کندل‌ها: Bitunix Public REST (بدون نیاز به API Key برای مارکت‌دیتا)
- داده بازار آزاد: CoinGecko/Alternative APIs (برای توکن‌های اسپات)

### همگام‌سازی بازار با CoinGecko
برای پر کردن لیست توکن‌ها و قیمت‌ها در داشبورد غیر فیوچرز، از CoinGecko استفاده کنید (بدون کلید):

```bash
# راه‌اندازی سرور
python dashboard_server.py

# در ترمینال جداگانه، همگام‌سازی 750 کوین برتر (3 صفحه × 250)
curl -X POST "http://localhost:5000/api/coingecko/sync?pages=3&vs_currency=usd"
```

اگر پشت NAT/فایروال هستید و با محدودیت مواجه شدید، دفعات را کمتر کنید (pages=1).

### افزودن صرافی جدید

برای افزودن صرافی جدید، کلاس `TradingBot` را گسترش دهید:

```python
def execute_trade_exchange_x(self, signal):
    # کد معامله در صرافی X
    pass
```

### بهبود الگوریتم‌های تشخیص

می‌توانید الگوریتم‌های تشخیص را در کلاس `PumpDumpDetector` بهبود دهید.

## مجوز

این پروژه برای استفاده آموزشی و تحقیقاتی است. استفاده از آن برای معامله واقعی به عهده خود شماست.

## هشدار

⚠️ **هشدار**: معامله در بازار کریپتو پرریسک است. این ربات تنها یک ابزار است و تضمینی برای سودآوری ندارد. همیشه با احتیاط عمل کنید و تنها سرمایه‌ای را به خطر بیندازید که توان از دست دادن آن را دارید.

## پشتیبانی

برای مشکلات و سوالات:
1. بررسی لاگ‌ها
2. بررسی مستندات
3. بررسی Issues در GitHub (اگر پروژه روی GitHub باشد)

## تغییرات و به‌روزرسانی

برای به‌روزرسانی:

```bash
git pull  # اگر از Git استفاده می‌کنید
pip install -r requirements.txt --upgrade
```

## تشکر

- DexScreener برای API عالی
- RugCheck برای بررسی امنیت توکن‌ها
- Bitunix برای پلتفرم معاملاتی

---

**نویسنده**: Advanced Crypto Trading Bot Team  
**نسخه**: 1.0  
**تاریخ**: 2024

