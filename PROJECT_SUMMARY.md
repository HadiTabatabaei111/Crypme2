# خلاصه پروژه - ربات ترید فیوچرز کریپتو

## ✅ ویژگی‌های پیاده‌سازی شده

### 1. ادغام با DexScreener ✓
- دریافت خودکار توکن‌ها و جفت‌های معاملاتی
- اسکن توکن‌های جدید و محبوب
- پشتیبانی از چندین بلاک‌چین (Ethereum, BSC, Polygon, Arbitrum, Optimism, Base)

### 2. تشخیص پامپ و دامپ ✓
- **تشخیص پامپ**: شناسایی افزایش قیمت ناگهانی (>15%) با حجم بالا
- **تشخیص دامپ**: شناسایی کاهش قیمت شدید (>15%)
- **تشخیص Shadow Pump**: شناسایی حجم جعلی و دستکاری قیمت
  - بررسی نسبت حجم به نقدینگی
  - تشخیص نوسانات غیرطبیعی
  - استفاده از الگوریتم‌های پیشرفته

### 3. بررسی RugCheck ✓
- بررسی امنیت توکن‌ها قبل از معامله
- تشخیص هانی‌پات (Honeypot)
- بررسی قابلیت مینت شدن
- بررسی قفل نقدینگی
- فقط معامله با توکن‌های "خوب" (Good)

### 4. لیست سیاه (Blacklist) ✓
- اضافه کردن خودکار توکن‌های مشکوک
- ذخیره دلیل اضافه شدن به لیست سیاه
- جلوگیری از معامله با توکن‌های لیست سیاه

### 5. ذخیره‌سازی داده در دیتابیس ✓
- **جدول tokens**: اطلاعات کامل توکن‌ها
- **جدول price_history**: تاریخچه قیمت‌ها
- **جدول trading_signals**: سیگنال‌های معاملاتی
- **جدول trades**: معاملات انجام شده
- **جدول patterns**: الگوهای شناسایی شده
- **جدول blacklist**: لیست سیاه توکن‌ها

### 6. معامله خودکار در ByBit Futures ✓
- ادغام کامل با ByBit Futures API
- پشتیبانی از معاملات LONG و SHORT
- مدیریت خودکار Stop Loss و Take Profit
- نظارت بر موقعیت‌های باز
- پشتیبانی از Testnet برای تست

### 7. تحلیل الگوها ✓
- شناسایی الگوهای قیمتی
- ذخیره الگوها برای تحلیل بعدی
- استفاده از تاریخچه قیمت برای تشخیص الگو

## 📁 ساختار فایل‌ها

```
New folder/
│
├── crypto_futures_bot.py      # کد اصلی ربات (850+ خط)
├── requirements.txt            # وابستگی‌های Python
├── config.json.example         # نمونه فایل پیکربندی
├── setup_and_run.sh           # اسکریپت نصب (Linux/Mac)
├── setup_and_run.bat          # اسکریپت نصب (Windows)
├── run_bot.sh                 # اسکریپت اصلی همه‌کاره
├── README.md                  # مستندات کامل (فارسی)
├── QUICKSTART.md              # راهنمای سریع
├── PROJECT_SUMMARY.md         # این فایل
│
├── trading_bot.db             # دیتابیس SQLite (پس از اجرا)
└── trading_bot.log            # فایل لاگ (پس از اجرا)
```

## 🚀 نحوه استفاده

### روش 1: استفاده از اسکریپت (توصیه می‌شود)

**Linux/Mac:**
```bash
chmod +x run_bot.sh
./run_bot.sh --run
```

**Windows:**
```bash
setup_and_run.bat --run
```

### روش 2: اجرای مستقیم

```bash
python crypto_futures_bot.py
```

## ⚙️ پیکربندی

### تنظیمات اولیه

1. کپی کردن فایل کانفیگ:
```bash
cp config.json.example config.json
```

2. ویرایش `config.json`:
```json
{
  "bybit_api_key": "YOUR_API_KEY",
  "bybit_api_secret": "YOUR_API_SECRET",
  "testnet": true,
  "auto_trade": false,
  "scan_interval": 300,
  "min_confidence": 0.7,
  "max_position_size": 100,
  "stop_loss_percent": 5,
  "take_profit_percent": 10
}
```

### پارامترهای مهم

- **testnet**: استفاده از ByBit Testnet (توصیه می‌شود برای شروع)
- **auto_trade**: فعال‌سازی معامله خودکار (false برای تحلیل فقط)
- **scan_interval**: فاصله اسکن (ثانیه) - پیش‌فرض: 300 (5 دقیقه)
- **min_confidence**: حداقل اعتماد برای سیگنال (0.0-1.0)
- **max_position_size**: حداکثر حجم معامله (USDT)
- **stop_loss_percent**: درصد Stop Loss
- **take_profit_percent**: درصد Take Profit

## 🔍 الگوریتم‌های تشخیص

### تشخیص پامپ
```python
- افزایش قیمت > 15% در بازه کوتاه
- افزایش حجم معاملات
- نقدینگی > $50,000
- حجم 24h > $50,000
```

### تشخیص دامپ
```python
- کاهش قیمت > 15%
- کاهش شدید حجم یا نقدینگی
- نقدینگی > $50,000
```

### تشخیص Shadow Pump
```python
- نسبت حجم به نقدینگی > 10
- نقدینگی < $50,000
- نوسانات بالا (>5%) با نقدینگی کم
- استفاده از الگوریتم‌های پیشرفته
```

## 🗄️ ساختار دیتابیس

### جدول tokens
- اطلاعات کامل توکن‌ها
- قیمت، حجم، نقدینگی
- وضعیت لیست سیاه

### جدول price_history
- تاریخچه قیمت‌ها
- حجم معاملات
- زمان ثبت

### جدول trading_signals
- نوع سیگنال (LONG/SHORT)
- سطح اعتماد
- قیمت ورود، Stop Loss، Take Profit
- دلیل سیگنال

### جدول trades
- معاملات انجام شده
- وضعیت (OPEN/CLOSED)
- سود/زیان

### جدول blacklist
- توکن‌های مشکوک
- دلیل اضافه شدن

### جدول patterns
- الگوهای شناسایی شده
- داده‌های الگو

## 🔒 امنیت

### ویژگی‌های امنیتی
1. بررسی RugCheck برای هر توکن
2. لیست سیاه خودکار
3. بررسی حجم جعلی
4. بررسی نقدینگی
5. Stop Loss خودکار
6. محدودیت حجم معامله

### توصیه‌های امنیتی
1. همیشه از Testnet شروع کنید
2. محدود کردن دسترسی API Key
3. استفاده از بخش کوچکی از سرمایه
4. نظارت مداوم
5. بکاپ منظم دیتابیس

## 📊 عملکرد ربات

### فرآیند کار
1. اسکن توکن‌ها از DexScreener
2. بررسی لیست سیاه
3. بررسی RugCheck
4. تحلیل قیمت و حجم
5. تشخیص پامپ/دامپ
6. تولید سیگنال
7. ذخیره در دیتابیس
8. اجرای معامله (در صورت فعال بودن)
9. نظارت بر موقعیت‌ها

### خروجی
- لاگ‌های کامل در `trading_bot.log`
- داده‌های ذخیره شده در دیتابیس
- سیگنال‌های معاملاتی
- گزارش عملکرد

## 🛠️ توسعه و سفارشی‌سازی

### افزودن صرافی جدید
کلاس `TradingBot` را گسترش دهید و متد `execute_trade` را برای صرافی جدید پیاده‌سازی کنید.

### بهبود الگوریتم‌ها
کلاس `PumpDumpDetector` را برای بهبود الگوریتم‌های تشخیص تغییر دهید.

### افزودن اندیکاتورهای جدید
می‌توانید اندیکاتورهای تکنیکال جدید اضافه کنید.

## ⚠️ هشدارها

1. **ریسک معامله**: معامله در بازار کریپتو پرریسک است
2. **بدون تضمین**: هیچ تضمینی برای سودآوری وجود ندارد
3. **تست اولیه**: همیشه قبل از معامله واقعی تست کنید
4. **نظارت مداوم**: ربات را تحت نظر داشته باشید
5. **مدیریت سرمایه**: فقط سرمایه قابل از دست دادن را استفاده کنید

## 📞 پشتیبانی

برای مشکلات و سوالات:
1. بررسی لاگ‌ها (`trading_bot.log`)
2. بررسی مستندات (`README.md`)
3. بررسی راهنمای سریع (`QUICKSTART.md`)

## 📝 تغییرات و به‌روزرسانی

### به‌روزرسانی
```bash
git pull  # اگر از Git استفاده می‌کنید
pip install -r requirements.txt --upgrade
```

### نسخه
- **نسخه فعلی**: 1.0
- **تاریخ**: 2024
- **وضعیت**: آماده برای استفاده

## ✨ ویژگی‌های خاص

1. **چند زنجیره**: پشتیبانی از چندین بلاک‌چین
2. **تحلیل پیشرفته**: الگوریتم‌های پیشرفته برای تشخیص پامپ/دامپ
3. **امنیت بالا**: بررسی چندلایه امنیت توکن‌ها
4. **ذخیره‌سازی کامل**: ذخیره تمام داده‌ها برای تحلیل
5. **معامله خودکار**: معامله خودکار با مدیریت ریسک
6. **لاگ کامل**: ثبت تمام فعالیت‌ها

## 🎯 نتیجه

ربات ترید فیوچرز کریپتو با تمام ویژگی‌های درخواستی پیاده‌سازی شده است:
- ✓ ادغام با DexScreener
- ✓ تشخیص پامپ، دامپ و Shadow Pump
- ✓ بررسی RugCheck
- ✓ لیست سیاه
- ✓ ذخیره‌سازی در دیتابیس
- ✓ معامله خودکار در ByBit Futures
- ✓ تحلیل الگوها
- ✓ مدیریت ریسک

ربات آماده استفاده است! 🚀

